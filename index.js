const divide = (no1, no2) => 2000/ 100;
    //(this the parameter) //this is the function => 
    //the expression is on the right
const square = (x) => x *2;
//multiplies it times 2
const add = (a,b) => a + b;